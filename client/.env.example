# GitHub OAuth App client ID (required)
VITE_GITHUB_CLIENT_ID=your_client_id

# API base URL — set when server runs on a different host/port
# In development, the Vite proxy forwards /api to the server automatically.
# Set this only when NOT using the Vite dev proxy (e.g. in production builds).
# VITE_API_URL=http://localhost:3001
