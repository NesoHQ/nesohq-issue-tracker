# Frontend does NOT require a .env file for connecting to the backend.
#
# To point the frontend at a backend server, edit:
#   client/public/config.js
#
# Set the apiBaseUrl value to your backend origin, for example:
#   window.APP_CONFIG = { apiBaseUrl: "https://api.yourdomain.com" };
#
# For local development the Vite dev proxy (/api → http://localhost:3001)
# handles the connection automatically without any config needed.
